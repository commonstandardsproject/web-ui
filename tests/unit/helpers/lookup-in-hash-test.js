import { lookupInHash } from '../../../helpers/lookup-in-hash';
import { module, test } from 'qunit';

module('Unit | Helper | lookup in hash');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = lookupInHash(42);
  assert.ok(result);
});
