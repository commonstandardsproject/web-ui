import { joinWords } from '../../../helpers/join-words';
import { module, test } from 'qunit';

module('Unit | Helper | join words');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = joinWords(42);
  assert.ok(result);
});
